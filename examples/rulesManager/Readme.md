# RulesManager example rules files

This directory contains a collection of rules files for the ComputePods
RulesManager.

It is organised by the rules files typically associated with each "type"
of ComputePod Chef.
